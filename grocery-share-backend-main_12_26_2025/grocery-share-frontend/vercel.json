{
  "installCommand": "npm install --force",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/fatsecret/:match*",
      "destination": "/api/fatsecret"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
